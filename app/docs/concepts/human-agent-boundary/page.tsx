import { Callout, DocsShell } from "@/components/docs-shell";

export default function BoundaryConcept() { return <DocsShell eyebrow="Explanation" title="Preserve the protection outcome, not the clicks." description="WebMCP should be faster than DOM automation. It must not become a shortcut around authentication, confirmation, spending limits, ownership rules, or fraud controls."><h2>The question Arena asks</h2><blockquote>Does the agent route enforce the protections that the human route was designed to enforce?</blockquote><p>A checkout UI may require review, final-price disclosure, authentication, and confirmation. A tool named <code>place_order</code> can bypass any of them while remaining perfectly valid against its JSON schema. Arena records both routes and compares their authoritative, settled effects.</p><h2>Parity has five dimensions</h2><ul><li><strong>Authorization:</strong> the same principal and policy authorize the effect.</li><li><strong>Approval:</strong> consequential intent receives equivalent confirmation.</li><li><strong>Value:</strong> money, recipients, resource owners, and limits remain within the review.</li><li><strong>Data:</strong> the agent cannot read or disclose more than the human route permits.</li><li><strong>Settlement:</strong> delayed writes and popup effects are observed through a terminal watermark.</li></ul><Callout title="Not a CAPTCHA"><p>CAPTCHAs ask whether an actor resembles a human. Arena asks whether an agent is authorized for this exact action and whether the site preserves that authority after execution.</p></Callout><h2>What the hosted proof establishes</h2><p>The current hosted demo binds a registered WebMCP callback request to an Arena-issued, single-use lease and joins it to server-attested effects. The proof does not cryptographically establish browser provenance or verify an external agent vendor. Those limits are explicit because trust claims are only useful when their boundary is inspectable.</p></DocsShell>; }
