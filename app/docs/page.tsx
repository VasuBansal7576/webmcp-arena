import { DocsShell } from "@/components/docs-shell";
import { ARENA_DOCS } from "@/src/docs-catalog.js";

export default function Docs() { return <DocsShell eyebrow="Documentation" title="Prove the boundary an agent actually crosses." description="Arena is a behavioral release gate for WebMCP. Start with a working proof, learn the trust model, then use the reference as a stable contract for agents and CI."><section className="docs-grid">{ARENA_DOCS.filter(({ kind }) => ["tutorial", "explanation", "reference"].includes(kind)).map((entry) => <a className="docs-card" href={entry.path} key={entry.path}><span>{entry.kind}</span><h2>{entry.title}</h2><p>{entry.summary}</p></a>)}</section><section><h2>Choose the document for the job</h2><p>The quickstart is a tutorial. The boundary chapter explains the model. Reference pages define exact schemas and failure behavior. Use cases show where the model pays off. Essays argue for the broader shift from bot detection to delegated authority.</p></section></DocsShell>; }
