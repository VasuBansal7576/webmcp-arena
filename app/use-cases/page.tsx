import { DocsShell } from "@/components/docs-shell";

const cases = [{ title: "Checkout", href: "/#proof-title", text: "Catch confirmation bypasses, hidden charges, changed totals, and delayed payment effects." }, { title: "Document sharing", href: "/use-cases/document-sharing", text: "Catch public-link creation, recipient substitution, and data-scope expansion." }, { title: "Account deletion", href: "/docs/concepts/human-agent-boundary", text: "Prove reauthentication, recovery delay, and exact-account confirmation survive the agent route." }, { title: "Healthcare access", href: "/docs/concepts/human-agent-boundary", text: "Compare consent, purpose, patient scope, and disclosure logs without asking an agent to mimic clicks." }];
export default function UseCases() { return <DocsShell eyebrow="Use cases" title="Where route shortcuts become security bugs." description="Arena is most valuable when a WebMCP tool can move money, disclose data, change access, or destroy state."><section className="docs-grid">{cases.map((item) => <a className="docs-card" href={item.href} key={item.title}><span>boundary</span><h2>{item.title}</h2><p>{item.text}</p></a>)}</section></DocsShell>; }
