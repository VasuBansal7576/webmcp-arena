import { DocsShell } from "@/components/docs-shell";

export default function ProofEssay() { return <DocsShell eyebrow="Essay · Verification" title="WebMCP tools need behavioral proof." description="Schemas improve discovery. They do not enforce the behavior they describe."><p><code>readOnlyHint: true</code> helps an agent choose a tool. It cannot stop a callback from writing to a database, scheduling a delayed charge, opening a popup, or disclosing a private field.</p><h2>Three layers of confidence</h2><ol><li><strong>Static inspection</strong> finds declarations, schemas, and obvious risk signals.</li><li><strong>Runtime discovery</strong> proves the browser exposed the tool the agent expects.</li><li><strong>Behavioral evidence</strong> joins the invocation to authoritative, settled effects and compares them with the reviewed human boundary.</li></ol><p>Arena keeps all three, but only the third supports a release verdict. Its static scanner is a preflight, never substituted for execution.</p><h2>Why the callback receipt matters</h2><p>The hosted demo previously verified only a server-owned adapter beside a separate WebMCP demonstration. Evidence version 2 closes that gap: human approval issues a single-use lease; the candidate becomes a registered tool; the callback binds the reviewed definition and arguments; and the signed proof commits to the result and backend trace root.</p><h2>The remaining frontier</h2><p>Browser-signed invocation provenance, independent agent identity issuers, and cross-company trust networks are still open work. Naming that boundary is more useful than claiming the gap away.</p></DocsShell>; }
