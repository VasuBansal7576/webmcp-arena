{
  "name": "agent-contract",
  "version": "0.1.0",
  "description": "Agent Contract OS: passive agent traffic analytics, static readiness checks, and .agent contract generation.",
  "type": "module",
  "bin": {
    "agent-contract": "./bin/agent-contract.js"
  },
  "files": [
    "README.md",
    "action.yml",
    "bin/",
    "docs/",
    "examples/",
    "schema/",
    "src/"
  ],
  "scripts": {
    "test": "node --test",
    "check": "npm test",
    "smoke": "node scripts/smoke-local.js",
    "release:check": "node scripts/release-check.js",
    "gate": "node ./bin/agent-contract.js gate"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT",
  "dependencies": {
    "playwright-core": "^1.61.0"
  }
}
